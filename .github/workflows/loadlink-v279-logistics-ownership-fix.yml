name: LoadLink V2.7.9 Logistics Ownership Fix

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/loadlink-v279-logistics-ownership-fix.yml'

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply structural Logistics Tools fix
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          tools_path = Path('components/LogisticsMessageTools.tsx')
          tools = tools_path.read_text()

          tools = tools.replace(
              '  trigger?: "bar" | "menu";\n',
              '  trigger?: "bar" | "menu" | "hidden";\n',
          )

          marker = '  }, [open]);\n\n  const templates = useMemo(() => {'
          insert = '''  }, [open]);

  useEffect(() => {
    if (trigger !== "hidden") return;
    const openFromChat = () => openPanel();
    window.addEventListener("loadlink:open-logistics-tools", openFromChat);
    return () => window.removeEventListener("loadlink:open-logistics-tools", openFromChat);
  }, [trigger, disabled, threadId]);

  const templates = useMemo(() => {'''
          if marker not in tools:
              raise SystemExit('LogisticsMessageTools open-effect marker not found')
          tools = tools.replace(marker, insert, 1)

          tools = tools.replace(
              '''  function openPanel() {
    if (typeof document !== "undefined" && document.activeElement instanceof HTMLElement) document.activeElement.blur();
    onOpen?.();
    setOpen(true);
  }
''',
              '''  function openPanel() {
    if (disabled) return;
    if (typeof document !== "undefined" && document.activeElement instanceof HTMLElement) document.activeElement.blur();
    onOpen?.();
    setOpen(true);
  }
''',
              1,
          )

          old_bar_start = ') : (\n        <button type="button" onClick={openPanel} disabled={disabled} className={`flex h-10 items-center gap-2 rounded-xl border px-3 text-xs font-black disabled:opacity-40 ${control}`} aria-expanded={open}>'
          new_bar_start = ') : trigger === "bar" ? (\n        <button type="button" onClick={openPanel} disabled={disabled} className={`flex h-10 items-center gap-2 rounded-xl border px-3 text-xs font-black disabled:opacity-40 ${control}`} aria-expanded={open}>'
          if old_bar_start not in tools:
              raise SystemExit('LogisticsMessageTools bar trigger marker not found')
          tools = tools.replace(old_bar_start, new_bar_start, 1)

          trigger_end = '''          Logistics
        </button>
      )}

      {open && typeof document !== "undefined" ? createPortal('''
          trigger_end_new = '''          Logistics
        </button>
      ) : null}

      {open && typeof document !== "undefined" ? createPortal('''
          if trigger_end not in tools:
              raise SystemExit('LogisticsMessageTools trigger end marker not found')
          tools = tools.replace(trigger_end, trigger_end_new, 1)
          tools_path.write_text(tools)

          page_path = Path('app/messages/page.tsx')
          page = page_path.read_text()

          return_marker = '''  return (
    <main
      data-theme={darkMode ? "dark" : "light"}'''
          return_replacement = '''  return (
    <>
      {selectedConversation ? (
        <LogisticsMessageTools
          threadId={selectedConversation.id}
          listingTitle={selectedConversation.listing_title}
          role={selectedConversation.role}
          darkMode={darkMode}
          disabled={sending || uploading || dailyLimitReached || conversationBlocked || potentialDealPending || potentialDealDeclined}
          trigger="hidden"
          onOpen={() => setComposerActionsOpen(false)}
          onSendQuote={sendStructuredQuote}
          quoteDefaults={quoteDefaults}
          savedVehicles={quoteVehicles}
          onInsert={(message) => updateTyping(text.trim() ? `${text.trim()}\n\n${message}` : message)}
        />
      ) : null}
      <main
      data-theme={darkMode ? "dark" : "light"}'''
          if return_marker not in page:
              raise SystemExit('Messages return marker not found')
          page = page.replace(return_marker, return_replacement, 1)

          old_component = '''                        <LogisticsMessageTools
                          threadId={selectedConversation.id}
                          listingTitle={selectedConversation.listing_title}
                          role={selectedConversation.role}
                          darkMode={darkMode}
                          disabled={sending || uploading || dailyLimitReached || conversationBlocked || potentialDealPending || potentialDealDeclined}
                          trigger="menu"
                          onClose={() => setComposerActionsOpen(false)}
                          onSendQuote={sendStructuredQuote}
                          quoteDefaults={quoteDefaults}
                          savedVehicles={quoteVehicles}
                          onInsert={(message) => updateTyping(text.trim() ? `${text.trim()}\n\n${message}` : message)}
                        />'''
          new_button = '''                        <button
                          type="button"
                          disabled={sending || uploading || dailyLimitReached || conversationBlocked || potentialDealPending || potentialDealDeclined}
                          onClick={() => {
                            setComposerActionsOpen(false);
                            window.dispatchEvent(new Event("loadlink:open-logistics-tools"));
                          }}
                          className="flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-xs font-bold hover:bg-black/[.04] disabled:opacity-40"
                        >
                          <span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-[#f6b800] text-black" aria-hidden="true">
                            <svg width="17" height="17" viewBox="0 0 24 24" fill="none"><path d="M3 7h11v9H3V7Zm11 3h3.4L21 13.6V16h-7v-6Z" stroke="currentColor" strokeWidth="1.8" strokeLinejoin="round" /><circle cx="7" cy="17.5" r="1.7" stroke="currentColor" strokeWidth="1.8" /><circle cx="17.5" cy="17.5" r="1.7" stroke="currentColor" strokeWidth="1.8" /></svg>
                          </span>
                          <span className="min-w-0 flex-1"><span className="block">Logistics tools</span><span className="mt-0.5 block truncate text-[9px] font-semibold text-black/40">Open the full tool workspace</span></span>
                        </button>'''
          if old_component not in page:
              raise SystemExit('Nested LogisticsMessageTools block not found')
          page = page.replace(old_component, new_button, 1)

          end_marker = '''      </div>
    </main>
  );
}

function VoiceAttachment'''
          end_replacement = '''      </div>
      </main>
    </>
  );
}

function VoiceAttachment'''
          if end_marker not in page:
              raise SystemExit('Messages return end marker not found')
          page = page.replace(end_marker, end_replacement, 1)
          page_path.write_text(page)
          PY

      - name: Install dependencies
        run: npm ci

      - name: Build validation
        run: npm run build

      - name: Commit structural repair
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/messages/page.tsx components/LogisticsMessageTools.tsx
          git commit -m "LoadLink V2.7.9: decouple logistics tools from chat composer"
          git push origin HEAD:main
