"use client";
import {useEffect,useState} from "react";import AdminShell from "@/components/admin/AdminShell";
export default function Page(){const [data,setData]=useState<Record<string,unknown>|null>(null),[error,setError]=useState("");useEffect(()=>{void fetch("/api/platform/health").then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);setData(d);}).catch(e=>setError(e instanceof Error?e.message:"Health check failed."));},[]);return <AdminShell title="Platform health" description="Live checks for the application, database connection and operational readiness.">{error?<p className="rounded-xl border border-red-500/40 p-4 font-bold text-red-500">{error}</p>:<pre className="overflow-auto rounded-2xl border border-current/10 p-5 text-sm">{JSON.stringify(data,null,2)}</pre>}</AdminShell>}
