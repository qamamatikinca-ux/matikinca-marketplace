"use client";
import {useState} from "react";import AdminShell from "@/components/admin/AdminShell";import {authenticatedFetch} from "@/lib/client/authenticatedFetch";import {useLoadLinkTheme} from "@/lib/useLoadLinkTheme";
export default function Page(){const {darkMode}=useLoadLinkTheme();const [form,setForm]=useState({userId:"",title:"",message:"",actionPath:"/account"}),[notice,setNotice]=useState("");async function send(){try{const r=await authenticatedFetch("/api/admin/notifications",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)throw new Error(d.error);setNotice("Notification sent and recorded in the audit trail.");}catch(e){setNotice(e instanceof Error?e.message:"Notification could not be sent.");}}const input=`rounded-xl border p-3 outline-none focus:border-[#f6b800] ${darkMode?"border-white/15 bg-white/5":"border-black/10 bg-white"}`;return <AdminShell title="Notification composer" description="Send safe account notifications with internal LoadLink links only. Every send is auditable."><section className="mx-auto grid max-w-2xl gap-3"><input value={form.userId} onChange={e=>setForm({...form,userId:e.target.value})} placeholder="Recipient user ID" className={input}/><input value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder="Notification title" className={input}/><textarea value={form.message} onChange={e=>setForm({...form,message:e.target.value})} placeholder="Clear message to the user" className={`${input} min-h-36`}/><input value={form.actionPath} onChange={e=>setForm({...form,actionPath:e.target.value})} placeholder="/account or /my-posts" className={input}/><button onClick={()=>void send()} className="h-12 rounded-xl bg-[#f6b800] font-black text-black">Send notification</button>{notice?<p className="rounded-xl border border-[#f6b800]/40 p-3 font-bold">{notice}</p>:null}</section></AdminShell>}
