"use client";
import Link from "next/link"; import {useEffect,useState} from "react"; import HomeLogoLink from "@/components/HomeLogoLink"; import {supabase} from "@/lib/supabaseClient";
type Status="not_started"|"phone_verified"|"pending"|"verified"|"rejected";
export default function StatusPage(){const [status,setStatus]=useState<Status>("not_started");const [reason,setReason]=useState("");const [loading,setLoading]=useState(true);useEffect(()=>{let channel:any;async function load(){const {data:{user}}=await supabase.auth.getUser();if(!user){setLoading(false);return;}const {data}=await supabase.from("verification_requests").select("status,rejection_reason").eq("user_id",user.id).maybeSingle();setStatus((data?.status||"not_started") as Status);setReason(data?.rejection_reason||"");setLoading(false);channel=supabase.channel(`verification-${user.id}`).on("postgres_changes",{event:"UPDATE",schema:"public",table:"verification_requests",filter:`user_id=eq.${user.id}`},(p:any)=>{setStatus(p.new.status);setReason(p.new.rejection_reason||"")}).subscribe();}load();const timer=setInterval(load,15000);return()=>{clearInterval(timer);if(channel)supabase.removeChannel(channel)}},[]);const copy={not_started:["Not started","Submit your phone number and identity documents to begin."],phone_verified:["Phone verified","Complete the document step to finish your application."],pending:["Pending review","Verification usually takes a few minutes when a reviewer is available. This page updates automatically."],verified:["Verified","Your identity has been approved. Your profile can now display the gold Verified badge."],rejected:["Needs attention",reason||"Your submission could not be approved. Review your information and submit again."]}[status];return <main className="min-h-screen bg-[#050505] px-5 py-8 text-white"><div className="mx-auto max-w-xl"><HomeLogoLink /><section className="mt-8 border border-white/10 bg-[#0b0b0b] p-7"><p className="text-xs font-black uppercase tracking-[.25em] text-[#f6b800]">Verification status</p><h1 className="mt-4 text-4xl font-black">{loading?"Checking status":copy[0]}</h1><p className="mt-4 leading-7 text-white/60">{loading?"Securely loading your latest verification result.":copy[1]}</p>{status==="verified"&&<span className="mt-7 inline-flex border border-[#f6b800] px-4 py-2 text-xs font-black uppercase tracking-[.16em] text-[#f6b800]">Verified</span>}{!loading&&status!=="verified"&&<Link href="/verify" className="mt-7 flex h-12 items-center justify-center bg-[#f6b800] font-black text-black">{status==="pending"?"Review submitted details":"Start or update verification"}</Link>}<Link href="/" className="mt-4 flex h-12 items-center justify-center border border-white/15 font-black">Return home</Link></section></div></main>}
