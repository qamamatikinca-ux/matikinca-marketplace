"use client";
export default function ErrorPage({ error, reset }: { error: Error & { digest?: string }; reset: () => void }) {
  return <main className="flex min-h-[100dvh] items-center justify-center bg-black p-5 text-white"><section className="w-full max-w-lg rounded-[28px] border border-[#f6b800]/40 bg-[#0b0b0b] p-7 text-center"><p className="text-xs font-black uppercase tracking-[.2em] text-[#f6b800]">LoadLink recovery</p><h1 className="mt-3 text-3xl font-black">This page could not finish loading</h1><p className="mt-3 text-sm leading-7 text-white/60">Your data was not submitted twice. Retry the page, or return home if the connection remains unavailable.</p><p className="sr-only">{error.message}</p><div className="mt-6 grid gap-2 sm:grid-cols-2"><button onClick={reset} className="h-12 rounded-xl bg-[#f6b800] font-black text-black">Try again</button><a href="/" className="flex h-12 items-center justify-center rounded-xl border border-white/15 font-black">Return home</a></div></section></main>;
}
