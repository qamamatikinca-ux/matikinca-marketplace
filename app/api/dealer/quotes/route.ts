import type { NextRequest } from "next/server";
import { apiError, dealerServerClient, requireDealerContext } from "@/lib/dealer/server";
export async function GET(request: NextRequest) { try { const client = dealerServerClient(request); await requireDealerContext(client); const sp = request.nextUrl.searchParams; if (sp.get("quote_id")) { const { data, error } = await client.rpc("loadlink_dealer_quote_view", { p_quote_id: sp.get("quote_id") }); if (error) throw error; return Response.json(data || {}); } const { data, error } = await client.rpc("loadlink_dealer_quote_list", { p_lead_id: sp.get("lead_id") || null }); if (error) throw error; return Response.json({ items: data || [] }); } catch (error) { return apiError(error); } }
export async function POST(request: NextRequest) { try { const client = dealerServerClient(request); await requireDealerContext(client); const body = await request.json(); if (String(body.action || "") === "share") { const { data, error } = await client.rpc("loadlink_dealer_quote_share", { p_quote_id: String(body.quote_id || "") }); if (error) throw error; return Response.json(data || {}); } const { data, error } = await client.rpc("loadlink_dealer_quote_action", { p_action: String(body.action || ""), p_payload: body }); if (error) throw error; return Response.json(data || { ok: true }); } catch (error) { return apiError(error); } }
